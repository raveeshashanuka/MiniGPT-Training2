data_dir: ./data
model_dir: ./models
batch_size: 16
learning_rate: 0.001
epochs: 5
embed_dim: 128
num_heads: 4
num_layers: 2
max_seq_len: 128
vocab_size: null  # Set during data prep
device: cuda if torch.cuda.is_available() else cpu
mixed_precision: true